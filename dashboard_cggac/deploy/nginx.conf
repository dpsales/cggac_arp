server {
    listen 80;
    server_name seu.dominio.gov.br;

    # Redireciona HTTP → HTTPS em produção
    # return 301 https://$host$request_uri;

    location = /favicon.ico { access_log off; log_not_found off; }

    location /static/ {
        alias /home/daianasales/vscode/datapipeline/dashboard_cggac/staticfiles/;
        expires 7d;
        add_header Cache-Control "public, immutable";
    }

    location / {
        include proxy_params;
        proxy_pass http://unix:/run/dashboard_cggac.sock;
        proxy_read_timeout 60s;
    }
}

# Bloco HTTPS (descomente e ajuste quando tiver certificado)
# server {
#     listen 443 ssl http2;
#     server_name seu.dominio.gov.br;
#     ssl_certificate     /etc/letsencrypt/live/seu.dominio.gov.br/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/seu.dominio.gov.br/privkey.pem;
#     include /etc/letsencrypt/options-ssl-nginx.conf;
#     ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
#
#     location = /favicon.ico { access_log off; log_not_found off; }
#     location /static/ {
#         alias /home/daianasales/vscode/datapipeline/dashboard_cggac/staticfiles/;
#     }
#     location / {
#         include proxy_params;
#         proxy_pass http://unix:/run/dashboard_cggac.sock;
#     }
# }
